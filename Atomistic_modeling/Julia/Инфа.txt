#=
typeof - тип переменной
split - делит по пробелу по дефолту
parse(тип в кот. хотим, сама переменная)
@. sin(cos(x))
x .+ x / x + x
x * x - плохо, x .* x - ок
extrema

function foo(x, y)
	return x, y
end

темы 1.7 и 1.10 не надо
=#

